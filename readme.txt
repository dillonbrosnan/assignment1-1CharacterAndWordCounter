compiled with DEV C++

ass1-1: takes in a string as a parameter, and counts the ammount of times each character is used, and prints out the least frequent letter.
user is then prompted for another string, and counts each time the word is used, and prints out each distinct word used
with its count.
